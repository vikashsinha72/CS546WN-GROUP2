{{! Simple extension to the main html file}}
{{! Remember that you don't full html structure for extensions}}

{{!-- <body>
    <h1>--- Insert Header Here---</h1>
    <main></main>
</body> --}}

    <head>
        <title>{{user.name}}</title>
    </head>
    <body>
        <article>
            <h1>{{user.name}}</h1>
            <h2>Events</h2>
            {{!-- <ul>
                {{#each character.comics.items}} -- REPLACE WITH USER EVENTS
                <li>{{this.name}}</li>
                {{/each}}
            </ul> --}}
            <h2>Reviews</h2>
             {{!-- <ul>
                {{#each character.comics.items}} -- REPLACE WITH USER REVIEWS
                <li>{{this.name}}</li>
                {{/each}}
            </ul> --}}
            <a href="/">Make Another Search</a>
        </article>
    </body>